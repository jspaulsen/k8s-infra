apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-route
  namespace: argocd
spec:
  parentRefs:
  - name: cloudflare-tunnel
    namespace: cloudflare-gateway
  hostnames:
  - gitops.yeebs.dev
  rules:
  - backendRefs:
    - name: argocd-server
      port: 80
